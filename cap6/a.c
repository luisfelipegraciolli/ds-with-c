#include <stdio.h>
#include <stdlib.h>

float *transposta(int m, int n, float *mat)
{

}

int main()
{
    int *p;
    int n;

    scanf("%d", &n);
    p[n];
    for (int i = 0; i < n; i++)
    {
       
        p[i] = 2*n;
    }
    for (int i = 0; i < n; i++)
    {
        printf("%d ", p[i]);
    }
    free(p);
}
